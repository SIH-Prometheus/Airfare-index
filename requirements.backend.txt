# =============================================================================
# PROMETHEUS MVP — Lean backend requirements (no Scrapy/Playwright)
# =============================================================================
# Core web framework
fastapi==0.111.0
uvicorn[standard]>=0.30.0

# Settings / validation
pydantic>=2.9.0
pydantic-settings==2.3.0
python-dotenv==1.0.1

# Database
sqlalchemy>=2.0.35
asyncpg==0.30.0

# MinIO object storage
minio>=7.2.0

# HTTP client (lightweight scraping)
httpx==0.27.0

# Utilities
structlog==24.2.0
tenacity==8.2.3
numpy>=1.26.0

# Auth (kept for future phases)
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1
email-validator>=2.0.0
polars>=1.0.0
